meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/tenants
  body: json
  auth: inherit
}

body:json {
  {
    "name":"testing tenant"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
