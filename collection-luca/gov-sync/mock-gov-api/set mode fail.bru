meta {
  name: set mode fail
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/__mock/gov-api/mode
  body: json
  auth: inherit
}

body:json {
  { "mode": "fail" }
}

settings {
  encodeUrl: true
  timeout: 0
}

