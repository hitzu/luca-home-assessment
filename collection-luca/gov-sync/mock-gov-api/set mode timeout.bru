meta {
  name: set mode timeout
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/__mock/gov-api/mode
  body: json
  auth: inherit
}

body:json {
  { "mode": "timeout" }
}

settings {
  encodeUrl: true
  timeout: 0
}

