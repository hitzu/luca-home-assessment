meta {
  name: trigger processing
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/tenants/{{tenantId}}/gov-sync/jobs/{{jobId}}/process
  body: none
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

