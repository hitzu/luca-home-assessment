meta {
  name: send batch should fast-fail (open)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/tenants/{{tenantId}}/gov-sync/__dev/gov-api/send-batch
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "periodId": "2025-Q3",
    "students": [
      { "studentId": "s-1", "payload": {} }
    ]
  }
}

vars {
  tenantId: Tenant1
}

settings {
  encodeUrl: true
  timeout: 0
}

